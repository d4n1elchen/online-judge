#include<bits/stdc++.h>
using namespace std;
typedef long long LL;

int main()
{
    ios::sync_with_stdio(false); cin.tie(0);

    int N;

    cin >> N;
    vector<int> B(N, 0);
    for(int i=0; i<N; ++i) cin >> B[i];
    sort(B.begin(), b.end());

    for(int i=0; i<N; ++i) {

    }

    return 0;
}

